import { GenderFromNationalIdPipe } from './gender-from-national-id.pipe';

describe('GenderFromNationalIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderFromNationalIdPipe();
    expect(pipe).toBeTruthy();
  });
});
