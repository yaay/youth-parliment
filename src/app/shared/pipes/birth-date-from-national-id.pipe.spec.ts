import { BirthDateFromNationalIdPipe } from './birth-date-from-national-id.pipe';

describe('BirthDateFromNationalIdPipe', () => {
  it('create an instance', () => {
    const pipe = new BirthDateFromNationalIdPipe();
    expect(pipe).toBeTruthy();
  });
});
